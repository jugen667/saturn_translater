int start = 0;
int end = 5;
int i;

void main () 
{
	// if((start > 65535) && (end > 255) && (i > 16) && true)
	if((start > 65535) && false) // NOT WORKING with a BOOL
	{
		i = 1 + (3+2);
	}
	else
	{
		i = 20;
	}
}
