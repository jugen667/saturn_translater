/* This file is a test
 * Comment on multiple 
 * line !
 */ 
int start = 0;
int x = 1;
// int end = 5;
// Starting main
void main(){
	// if(start < 0 && x != 0)
	// if(true)
	if(start < 0)
	{
		x = x + 1;
		x = 2*(1+2);
	}
	// start = ~(x*(4-3))+2;
/*  int i;
	int sum = 0;
	for (i = start; i < end; i = i + 1) {
 		sum = sum + 10;	
 	}
 	printf("sum :", sum , "\n"); */
}
