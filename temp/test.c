/* This file is a test
 * Comment on multiple 
 * line !
 */ 
int start = 0;
float x = 1;
bool e = true;
// int end = 5;
// Starting main
void main(){
	// if(start < 0 && x != 0)
	// if(true)
		start = 32;
	if(start > 3 || x > 1 || start == x) 
	{
		x = x & start;
		x = 2*(1+2);
	}
	else
	{
		x = 0;
	}
	// start = ~(x*(4-3))+2;
/*  int i;
	int sum = 0;
	for (i = start; i < end; i = i + 1) {
 		sum = sum + 10;	
 	}
 	printf("sum :", sum , "\n"); */
}
