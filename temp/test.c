/* This file is a test
 * Comment on multiple 
 * line !
 */ 
int start = 0;
int x = 1;
// int end = 5;
// Starting main
void main(){
	// if(start < 0 && x != 0)
	// if(true)
		start = 32;
	if(start < 0)
	{
		if(x > 2)
		{
			// x = x + 1;
			// x = 2*(1+2);
			start = 1;
			if (true)
			{
				x=x+2;
			}
		}
	}
	else
	{
		x = 0;
	}
	// start = ~(x*(4-3))+2;
/*  int i;
	int sum = 0;
	for (i = start; i < end; i = i + 1) {
 		sum = sum + 10;	
 	}
 	printf("sum :", sum , "\n"); */
}
